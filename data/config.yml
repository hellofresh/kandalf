---
log:
  level:                "info"
  #logstash:
    # "udp" or "tcp" supported
    #protocol:             "udp"
    # Address should be in "<host>:<port>" format
    #address:              "logstash:888"
kafka_brokers:
  # Docker
  - "kafka:9092"
rabbitmq_hosts:
  # Docker
  - "amqp://rmq"
redis:
  host:                 "redis"
worker:
  max_queue_size:       10
  sleep_timeout:        3
